#include <stdio.h>
int main() {
    int n = 5;
    int arr[] = {10, 20, 30, 40, 50};

    printf("Array elements are:\n");
    for(int i = 0; i < n; i++) {
        printf("%d ", arr[i]);
    }
    return 0;
}
