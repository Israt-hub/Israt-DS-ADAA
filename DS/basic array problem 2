#include <stdio.h>
int main() {
    int n = 4;
    int arr[] = {5, 10, 15, 20};
    int sum = 0;
    float average;

    for(int i = 0; i < n; i++) {
        sum += arr[i];
    }
    average = (float)sum / n;
    printf("Sum = %d\n", sum);
    printf("Average = %.2f\n", average);
    return 0;
}
